# .cargo/config.toml
# ESP32-S3 uses the Xtensa LX7 core
# Requires the xtensa-esp32s3-espidf toolchain installed by espup

[build]
target = "xtensa-esp32s3-espidf"

[target.xtensa-esp32s3-espidf]
linker = "ldproxy" # ESP-IDF link wrapper
runner = "espflash flash --flash-mode dout --flash-freq 20mhz --flash-size 16mb --monitor"

[env]
# esp-idf-sys needs to know the ESP-IDF version
ESP_IDF_VERSION = "v5.2.3"
# If you installed ESP-IDF with espup, the path is usually:
# ESP_IDF_SDKCONFIG_DEFAULTS = "sdkconfig.defaults"

[unstable]
build-std = ["std", "panic_abort"]    # Rust std for ESP-IDF
